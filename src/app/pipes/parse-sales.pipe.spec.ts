import { ParseSalesPipe } from './parse-sales.pipe';

describe('ParseSalesPipe', () => {
  it('create an instance', () => {
    const pipe = new ParseSalesPipe();
    expect(pipe).toBeTruthy();
  });
});
